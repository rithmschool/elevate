language: node_js
node_js:
  - '10'
services:
  - postgresql
before_script:
  - psql -c 'create database elevate;' -U postgres
  - psql < backend/data.sql
before_install:
- cd backend
install:
- npm install  
- npm install --save-dev jest
script:
- npm test